/* Internal Imports */
import { getNullString } from './misc'

export const NULL_ADDRESS = getNullString(32)
export const NULL_HASH = getNullString(64)
export const STATE_ID_LENGTH = 16 // length in bytes
